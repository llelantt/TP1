--TP1
--LELANT Louise

1. La commande git status permet d'afficher le statut, l'état du fichier dans lequel je suis. Elle permet également de voir les différentes modifications. 

2. Par rapport au premier git, je peux maintenant voir mon fichier réponses.txt que je viens d'ajouter.
	
3. Ils sont partis dans le nouveau répertoire courant du git.

4. J'ai utilisé la commande "git log" pour afficher la liste. 

6. Ces deux commandes comparent des modifications mais pas dans les mêmes zones. La commande git diff --staged affiche les différences dans le staging area.
diff --git a/README.md b/README.md
index 87d5ec1..4d01fe9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,3 @@
 # TP1 git - LELANT Louise
+Sur la branche develop
+rien à valider, la copie de travail est propre

7. La commande git show permet d'afficher l'historique des différents commits, en montrant les informations relatives à celui-ci de manière détaillée et complète, contrairement au git log. Elle affiche par exemple toutes les lignes qui ont été modifiées. Par exemple, elle m'affiche ceci, en plus des informations concernant sa création : diff --git a/README.md b/README.md
index 87d5ec1..4d01fe9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,3 @@
 # TP1 git - LELANT Louise
+Sur la branche develop
+rien à valider, la copie de travail est propre

  
La commande git status indique l'état actuel du dépôt git, ainsi que la brancge sur laquelle on se trouve. 

Git diff affiche les différences entre deux commits indiqués lors de la saisie de la commande, ainsi que leur modifications.
 
Pour finir, la commande git log permet d'afficher la liste des commits, avec juste les éléments importants de sa création, comme l'auteur et la date, ainsi que le message laissé.
Elle me donne par exemple ceci : commit 0725733023380a960ce89cd5f31f59390a629a96 (HEAD -> develop)
Author: llelantt <llelant@b109-06.iut.univ-paris8.fr>
Date:   Tue Apr 8 15:40:33 2025 +0200

    Ajout des nouveaux fichiers
 
8. Le résultat de la commande m'indique que j'ai deux nouveaux fichiers dans mon répertoire : Sur la branche develop
Modifications qui seront validées :
  (utilisez "git restore --staged <fichier>..." pour désindexer)
	nouveau fichier : brouillon.txt
	nouveau fichier : fichier1.txt

9. La commande git reset permet de réinitialiser le répertoire dans lequel je suis par rapport au fichier que j'indique. Elle annule les changements faits qui peuvent parfois être des erreurs. Les espaces concernés par cette commande sont le staging area ou les commits. 

10. Pour cela, je fais un git log brouillon.txt et m'assure que rien ne s'affiche. C'est le cas, cela signifie donc que mon fichier n'a pas été commit dans le staging area. Après le commit du fichier1.txt, elle m'affiche les informations de celui-ci : 
commit 9f5c5f3d1c89806778a7874fd2b79858f60e6797 (HEAD -> develop)
Author: llelantt <llelant@b109-06.iut.univ-paris8.fr>
Date:   Tue Apr 8 16:31:08 2025 +0200

    fichier1.txt

11. Je fais d'abord un git status pour analyser l'état de mon répertoire. Je fais ensuite un git log pour afficher tous mes commits et voir si ils y sont tous. 
 
13. Je fais un git clone pour cloner ce projet. 

14. Je trouve le fichier README.md, et je trouve ces intitulés : Initial commit, Ajout d'une 2e ligne dans le fichier README.md., Ajout dans README.md, Modification sur la deuxième ligne du fichier README.md pour que celle-ci s'affiche comme du texte et non un titre., Correctif README.md.
J'ai utilisé la commande git log. 

15. Pour le commit f7a7, la ligne "Ceci est un fichier README.md" a été ajoutée, et la ligne "Ceci est un fichier README.md" a été retirée par rapport au commit f813. Pour le commit 9b68, la ligne "TP-Git" a été ajoutée et la ligne "Ceci est un fichier README.md" par rapport au commit f813. 
J'ai fait des git diff suivis des deux commits que je voulais comparer. 
 

 

